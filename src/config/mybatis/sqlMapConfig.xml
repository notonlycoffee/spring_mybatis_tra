<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
 PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>


	<!-- <properties resource="config/db.properties" /> -->


	<!-- <settings>
		 配置懒加载
		<setting name="lazyLoadingEnabled" value="true" />
		<setting name="aggressiveLazyLoading" value="false" />
	</settings> -->
	

	<typeAliases>

		<package name="com.wwq.frame.domain.Person" />

	</typeAliases>

	<!-- <environments default="development">
		<environment id="development">
			<transactionManager type="JDBC" />
			<dataSource type="POOLED">
				<property name="driver" value="${jdbc.driver}" />
				<property name="url" value="${jdbc.url}" />
				<property name="username" value="${jdbc.username}" />
				<property name="password" value="${jdbc.password}" />
			</dataSource>
		</environment>
	</environments> -->

	<mappers>
		 <!-- <mapper resource="config/mapper/User.xml" /> -->
		 
		 <!-- <mapper resource="config/mapper/UserMapper.xml" /> -->

		<!-- <mapper class="com.adaque.wwq.mybatis_maven.mapper.UserMapper"/> -->

		<!-- <package name="com.adaque.wwq.mapper" /> -->
	</mappers>

</configuration>